kind = "web"
previewPath = "/"
title = "Crew Scheduler"
version = "1.0.0"
id = "artifacts/crew-scheduler"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19262

[services.development]
run = "pnpm --filter @workspace/crew-scheduler run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/crew-scheduler", "run", "build" ]
publicDir = "artifacts/crew-scheduler/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19262"
BASE_PATH = "/"
